# Common properties shared across all environments
# DO NOT put secrets here - this file is typically committed to version control

spring:
  application:
    name: gayale-transport
  data:
    mongodb:
      #uri: mongodb+srv://fmoussa:kallah@cluster0.yy3ajm3.mongodb.net/gayale_transport_dev?retryWrites=true&w=majority
      uri: mongodb://localhost:27017/gayal_transport_dev_local?retryWrites=true&w=majority
      auto-index-creation: true

server:
  port: 8080

logging:
  level:
    root: WARN
    com.gayale.transport: DEBUG
    org.springframework.data.mongodb: WARN
    org.springframework.security: WARN

app:
  qrcode:
    base-url: ${QRCODE_BASE_URL:http://localhost:4200/tickets/}
  security:
    jwt:
      expiration: 86400000
      refresh-expiration: 604800000 # 7 days in milliseconds
      # Le secret doit être fourni via la variable d'environnement JWT_SECRET.
      # La valeur de repli ci-dessous ne sert QU'AU développement local.
      secret: ${JWT_SECRET:VtH7!aKz9#NnXq3$Pf@wJd5^Rb2ZsLmY%UgE0Cq*XiTp+AdGhKrMb4YuJeVo6WxB}
